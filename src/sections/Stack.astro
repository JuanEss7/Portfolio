---
import { FaCircleCheck } from "react-icons/fa6";
import { LuLoader } from "react-icons/lu";
const skills = [
    {
        aprendido: true,
        lenguaje: "HTML",
    },
    {
        aprendido: true,
        lenguaje: "CSS",
    },
    {
        aprendido: true,
        lenguaje: "JavaScript",
    },
    {
        aprendido: true,
        lenguaje: "Tailwind",
    },
    {
        aprendido: true,
        lenguaje: "ReactJs",
    },
    {
        aprendido: true,
        lenguaje: "TypeScript",
    },

    {
        aprendido: true,
        lenguaje: "NextJs",
    },
    {
        aprendido: true,
        lenguaje: "Astro",
    },
    {
        aprendido: true,
        lenguaje: "NodeJS",
    },
    {
        aprendido: true,
        lenguaje: "Express",
    },
    {
        aprendido: false,
        lenguaje: "Prisma",
    },
    {
        aprendido: false,
        lenguaje: "Firebase",
    },
];
---

<section class="container">
    <h2>Stack</h2>
    <ul>
        {
            skills.map((skill) => {
                return (
                    <li>
                        {skill.aprendido ? (
                            <FaCircleCheck size={18} />
                        ) : (
                            <LuLoader size={18} />
                        )}
                        {skill.lenguaje}
                    </li>
                );
            })
        }
    </ul>
</section>
<style>
    .container {
        padding: 3rem 0;
        height: clamp(fit-content, 60dvh, max-content);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 3rem;
    }
    h2 {
        font-size: var(--h1-font-size);
        font-weight: var(--font-semi-bold);
        color: var(--title-color);
    }
    .container ul {
        width: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-wrap: wrap;
        gap: 1rem;
        & > li {
            border-radius: 15px;
            padding: var(--mb-1);
            background-color: var(--container-color);
            box-shadow: 0px 0px 15px #0002;
            display: flex;
            align-items: center;
            gap: 4px;
        }
    }

    @media screen and (width <= 790px) {
        #about {
            height: fit-content;
        }
        .informacion {
            flex-direction: column;
            & .educacion,
            & .skills {
                width: 100%;
                height: fit-content;
            }
        }
        .skills {
            height: fit-content;
            display: flex;
            align-items: center;
            justify-content: center;
            & .container-skills {
                /* border: 1px solid purple; */
                justify-content: center;
            }
        }
    }
</style>
